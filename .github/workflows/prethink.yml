name: Prethink Context Generation

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  prethink:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17 (Zulu)
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      # Workaround: Gradle plugin 7.29.0 lacks the exists() check from
      # openrewrite/rewrite-gradle-plugin#431, so it crashes when deleting
      # files that were only in-memory. Only calm-architecture.json needs
      # this — architecture.md must NOT be pre-created as it prevents
      # GenerateCalmMermaidDiagram from generating the Mermaid diagram.
      - name: Work around Gradle plugin delete bug
        run: |
          mkdir -p .moderne/context
          touch .moderne/context/calm-architecture.json

      - name: Run Prethink (no AI) and custom recipe
        run: ./gradlew :app:rewriteRun --refresh-dependencies

      - name: Verify generated context files
        run: |
          echo "=== Generated Prethink context files ==="
          find .moderne/context -type f 2>/dev/null || echo "No .moderne/context directory found"
          echo ""
          echo "=== Agent config files ==="
          for f in AGENTS.md CLAUDE.md .cursorrules .github/copilot-instructions.md; do
            if [ -f "$f" ]; then
              echo "Found: $f"
            fi
          done
          echo ""
          echo "=== Data tables ==="
          find build/reports/rewrite -type f 2>/dev/null || echo "No data tables exported"

      - name: Verify architecture context
        run: |
          DIAGRAM=".moderne/context/architecture.md"
          if [ ! -s "$DIAGRAM" ]; then
            echo "ERROR: $DIAGRAM is missing or empty"
            exit 1
          fi
          if ! grep -q '```mermaid' "$DIAGRAM"; then
            echo "ERROR: $DIAGRAM does not contain a Mermaid diagram"
            echo "--- contents ---"
            cat "$DIAGRAM"
            exit 1
          fi
          echo "Architecture context verified:"
          cat "$DIAGRAM"
